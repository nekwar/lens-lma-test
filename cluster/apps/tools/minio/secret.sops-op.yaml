# yamllint disable
# apiVersion: v1
# kind: Secret
# metadata:
#     name: minio-helm-values
#     namespace: tools
# type: Opaque
# stringData:
#     values.yaml: |
#         accessKey:
#         secretKey:
apiVersion: v1
kind: Secret
metadata:
    name: minio-helm-values
    namespace: tools
type: Opaque
stringData:
    values.yaml: |
        accessKey: ${SECRET_MINIO_ACCESS_KEY}
        secretKey: ${SECRET_MINIO_SECRET_KEY}